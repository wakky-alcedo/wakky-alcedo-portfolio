<%*
// ガード句：テンプレートフォルダ内のファイル、または Untitled 以外のファイルなら実行しない 
if (!tp.file.title.includes("Untitled")) { return; }

// ファイル名
let filename = await tp.system.prompt("ファイル名を入力してください");
if (filename == null || filename == "") {
  filename = tp.file.title;
}
const targetFolder = "src/content/works";
await tp.file.move(`${targetFolder}/${filename}`);

// タイトル
// 1. 入力ダイアログを表示  
let title = await tp.system.prompt("タイトルを入力してください");  
if (title == null || title == "") { title = tp.file.title; }  
%>---
title: <% title %>
description: 
date: <% tp.file.creation_date() %>
thumbnail: ../../assets/images/thumbnail_work1.webp
tags:
  - 
wip: true
unpublished: true
---
import CustomImage from '../../components/content/CustomImage.astro';
import FlexRow from '../../components/content/FlexRow.astro';
import YouTube from '../../components/content/YouTube.astro';

## 概要

ユーザーに何を思ってもらいたいか
企画の概要
- 制作期間：
- 担当範囲：

## 開発の思いor背景・課題


## 製作

### 使用ツール・環境

### こだわり

## 成果・実績

## 今後の課題or今後の展望

## 関連リンク
- 公式Twitter：
- 公式Instagram：
- 当時の報告記事：
- GitHubリポジトリ：